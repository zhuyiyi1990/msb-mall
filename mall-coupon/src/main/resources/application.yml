spring:
    datasource:
        username: root
        password: password
        url: jdbc:mysql://192.168.248.100:3306/mall-sms
        driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
    mapper-locations: classpath*:/mapper/**/*.xml
    global-config:
        db-config:
            id-type: auto

server:
    port: 8010